<% if @negotiates.any? %>
  <%= render partial: 'books/part',
    collection: @negotiates, as: "object", locals: { link: true } %>
<% else %>
  <div class="alert alert-danger" role="alert">
    現在交渉中の取引はありません。
  </div>
<% end %>
