<%= bootstrap_form_for [@book, @negotiate] do |f| %>
  <%= f.hidden_field :parent_id, value: @book.id %>
  <%= f.collection_select :child_id, @books, :id, :title %>
  <%= f.text_area :recommend %>
  <%= f.submit %>
<% end %>
