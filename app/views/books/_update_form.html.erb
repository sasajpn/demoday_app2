<%= bootstrap_form_for(object) do |f| %>
  <%= f.hidden_field :status, value: num %>
  <%= f.submit update_title(num), class: "btn btn-primary btn-sm", data: { confirm: confirm_message(num) }%>
<% end %>
