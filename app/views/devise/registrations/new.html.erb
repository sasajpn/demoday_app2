<div class="col-md-4 col-sm-8 col-xs-10 col-md-offset-4 col-sm-offset-2 col-xs-offset-1">
  <%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "form-horizontal text-left"}, inline_errors: false) do |f| %>
    <%= devise_error_messages! %>
    <%= f.text_field :username, hide_label: true, placeholder: 'ユーザネーム' %>
    <%= f.email_field :email, hide_label: true, placeholder: 'メールアドレス' %>
    <div class="birthday-select">
      <%= f.label :birthday, '生年月日（※登録後の変更はできません）' %>
      <%= f.date_select :birthday, hide_label: true, use_month_numbers: true, default: '1990-1-1'.to_date, start_year: 1920, end_year: Time.now.year %>
    </div>
    <%= f.password_field :password, hide_label: true, placeholder: 'パスワード' %>
    <%= f.password_field :password_confirmation, hide_label: true, placeholder: 'パスワード(確認用)' %>
    <%= f.submit '新規登録' %>
    <br>
  <% end %>
  <p>既にアカウントをお持ちですか？<%= link_to new_session_path(resource_name) do %>ログイン<% end %></p>
  <p>メールが届きませんか？<%= link_to new_confirmation_path(resource_name) do %>メールの再送信<% end %></p>
</div>
