<%= bootstrap_form_for [@wish, @reply] do |f| %>
  <%= f.hidden_field :book_id, value: book.id %>
  <%= f.submit 'これがいいな！', data: { confirm: "これがいいな！してもよろしいですか？" }, class: 'btn btn-primary btn-sm' %>
<% end %>
